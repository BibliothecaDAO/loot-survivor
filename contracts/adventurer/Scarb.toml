[package]
name = "survivor"
version = "0.1.0"

# See more keys and their definitions at https://docs.swmansion.com/scarb/docs/reference/manifest

[dependencies]
starknet = ">=2.0.0-rc0"
pack = { path = "../pack" }
lootitems = { path = "../loot" }